{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach to dev",
            "protocol": "inspector",
            "port": 7001,
            "restart": true,
            "cwd": "${workspaceRoot}"
        },
    ]
}